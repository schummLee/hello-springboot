# 应用服务 WEB 访问端口
server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://mysqlServer:3306/${DB_NAME}?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USER}
    password: ${DB_PASSWORD}
